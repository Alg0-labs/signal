generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model WalletSnapshot {
  address   String   @id
  payload   Json
  updatedAt DateTime @updatedAt
}
